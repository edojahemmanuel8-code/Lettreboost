name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          VITE_APPS_SCRIPT_URL: https://script.google.com/macros/s/AKfycbwyLIwyUianNM8y_PCh7v7mkjgJkdR3P1f9Be5rOqIgvScYu76oR-RipjzE9hqH4tTr/exec
          VITE_WHATSAPP_NUMBER: 2347043463441
          VITE_CONTACT_EMAIL: admin@lettreboost.com.ng
          VITE_GA4_MEASUREMENT_ID: G-ETC4KHBQQV
          VITE_TAWKTO_PROPERTY_ID: 6a981431c8da5c3443c0f516
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
